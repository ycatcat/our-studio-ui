import { Loading } from "element-ui"
const loadOption = {
  fullscreen:true,
  lock:true,
  text:"数据加载中",
};
export default class loadEventloadEvents {
  constructor(vueThis){
    this.vm = vueThis;

  }
}
